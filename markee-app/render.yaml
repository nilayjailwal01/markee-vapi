services:
  - type: web
    name: markee-app
    env: node
    buildCommand: npm ci && npm run build
    startCommand: serve -s build
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    healthCheckPath: /
    autoDeploy: true 